/** Layout padrão de formulários densos de campanha (3 colunas no desktop). */
export const FORM_GRID_CLASS =
  "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3";

/** Campo que ocupa a linha inteira (ex.: observações). */
export const FORM_SPAN_FULL = "sm:col-span-2 lg:col-span-3";

/** Campo que ocupa 2 colunas no desktop. */
export const FORM_SPAN_2 = "lg:col-span-2";
